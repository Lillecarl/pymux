# Every difference between a picture of vttest with pymux in the chain
# and one without it. `tests/photograph_vttest.py` says what this is and
# how to write it.
#
# A line is a terminal, the screen by the menu path that reached it, and
# the verdict on it. The path is what `paths.txt` names, so a picture and
# a line here can always be lined up.
#
# A still screen is judged in pixels, and the verdict is how many differ.
# A blinking screen is judged twice: the same way, on a picture taken
# after its burst, and again on whether it blinks at all, under the same
# name with "(blink)" after it.
#
# Both are written down here, because a gate that is red for a reason
# everybody already knows is a gate everybody learns to ignore
# (Lillecarl/pymux#46). This check is not a gate yet either way: the
# chain is proven and the judging is not, and `photograph_vttest.py`
# says which part still flaps.
#
# Not every difference is a fault. A pane reads what a program asked for
# and writes the request again in a form the terminal of the user
# understands, so a pane can draw more than that terminal draws on its
# own, and it cannot do some of the things a terminal does to itself.
#
# A count is a number of pixels, so it belongs to the font, the size and
# the geometry that `take_picture.py` asks each terminal for. Change
# any of them and every count has to be recorded again.
#
# Item 4 of vttest's main menu is recorded, and item 9. Item 2 is
# measured and not written down, because two runs of it do not yet
# agree.
#
# **Record both items or lose one.** The run writes the list of what it
# saw, and the default walks item 4 alone, so a plain
# `checks.pymux-vttest-pictures.run` writes a file with no item 9 in
# it. The command that keeps both:
#
#     PYMUX_VTTEST_INCLUDE='^(4|9) ' \
#       nix build --file . checks.pymux-vttest-pictures.run

# Item 4 of vttest is entirely about the DEC line attributes, and a
# pane does not have them. "ESC # 3", "ESC # 4", "ESC # 5" and
# "ESC # 6" draw a line at twice the width or twice the height on a
# VT100. kitty, Ghostty and Alacritty implement none of them, esctest
# has no test for them, and no recorded program in this repository
# sends one. So a pane reads each of the four and draws the line the
# plain way. Lillecarl/pymux#141.
#
# Every count below is that, and it is the whole of every count. What
# a person sees: xterm draws each of these lines with glyphs twice as
# big, and the pane draws the same characters at their ordinary size.
# A double height line shows its text on both of its rows, because the
# program writes the text once for each half.
#
# These are the biggest counts in this file by far, and they are the
# least interesting. Six screens that are about one feature nobody
# implements say nothing else about the pane.
xterm 4 Test of double-sized characters #1 15873
xterm 4 Test of double-sized characters #2 18859

# The same, and DECCOLM as well. vttest asks for 132 columns, and
# xterm answers by making its window wider: the bare picture is 132
# cells across and 1280 pixels wide. A pane cannot do that. It has the
# cells the user gave it, and nothing in the protocol lets a program in
# a pane resize the window of the person running it, so the pymux
# picture stays 80 cells and 800 pixels. That is not a fault either: a
# multiplexer exists because the terminal belongs to the user.
#
# `differences` crops the two pictures to the rectangle they share and
# measures that, so the 480 pixels of width that only one of them has
# are not in the count.
xterm 4 Test of double-sized characters #3 14774
xterm 4 Test of double-sized characters #4 17760

# The same again, on a screen that also blinks.
xterm 4 Test of double-sized characters #5 20925

# "Exactly half of the box should remain." vttest draws a box and makes
# its rows double width, so xterm shows the half of it that fits. The
# pane draws the whole box at its ordinary size.
#
# The cursor of this screen used to be two columns to the left of where
# xterm puts it (Lillecarl/pymux#115). It is not any more: the top row
# of the two pictures is identical to the pixel. The doubling was what
# moved it.
xterm 4 Test of double-sized characters #6 5626

# Item 9 is the screens that expose xterm's own bugs, one screen per
# bug, and the pane is not xterm: its emulator answers each question
# its own way, and the difference pictures are grey across most of the
# screen because the bug's behaviour is what the screen is of. The
# scroll region moves whole lines in the pane the way the emulator
# defines it; a double-wide line is drawn at ordinary size, so a
# cursor moving off one lands a row's width from where xterm puts it.
# Every count is the emulator's answer, not a fault of pymux, and the
# pictures to read are the run's `differ/` directory.
# Lillecarl/pymux#280.
xterm 9 Test of known bugs / 2 Bug B: Scrolling region #1 4362
xterm 9 Test of known bugs / 2 Bug B: Scrolling region #2 4898

# **Three screens of item 9 draw closer to xterm than they did**, and
# nothing said so. A count can never be smaller than the channel error
# sum of the same two pictures, so a number that went *down* when the
# instrument changed is a picture that changed:
#
#     5 Bug E #1   15713 -> 11716
#     8 #4          2598 ->  2273
#     8 #6          2598 ->  2273
#
# Two runs of these sixteen screens, built separately, wrote the same
# list to the byte, so it is not a flap. Whatever fixed them landed
# while this check was not a gate and nobody was reading it.
xterm 9 Test of known bugs / 5 Bug E: Cursor move from double- to single-wide line #1 11716

# One and two pixels. These three were recorded as no difference at
# all, and they were never that: the old instrument weighed them at
# under one whole channel of error over an 800 by 480 picture and
# truncated it to zero. Lillecarl/pymux#368.
xterm 9 Test of known bugs / 7 Wrap around with cursor addressing #1 1
xterm 9 Test of known bugs / 8 Erase right half of double width lines #1 2
xterm 9 Test of known bugs / 8 Erase right half of double width lines #3 2

xterm 9 Test of known bugs / 8 Erase right half of double width lines #2 4570
xterm 9 Test of known bugs / 8 Erase right half of double width lines #4 2273
xterm 9 Test of known bugs / 8 Erase right half of double width lines #5 6841
xterm 9 Test of known bugs / 8 Erase right half of double width lines #6 2273
